defmodule Problem25 do
  @moduledoc """
  주어진 맵에서 키에 해당하는 값을 안전하게 반환하는 함수를 구현합니다.

  키가 존재하면 `{:ok, value}` 튜플을 반환하고,
  키가 존재하지 않으면 `{:error, :key_not_found}` 튜플을 반환합니다.
  """
  # 여기에 safe_map_access/2 함수를 구현하세요.
end
