defmodule Problem12 do
  @moduledoc """
  두 개의 문자열을 연결하는 함수를 구현합니다.
  """
  # 여기에 concatenate/2 함수를 구현하세요.
  @spec concatenate(String.t(), String.t()) :: String.t()

  def concatenate(a, b), do: a <> b
end
